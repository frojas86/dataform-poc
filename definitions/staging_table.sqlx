
config { 
    type: "view",
    description: "This is a staging view that selects from the source data."
}

SELECT 
    event_id,
    user_id,
    event_timestamp,
    event_date
FROM ${ref("source_data")}
